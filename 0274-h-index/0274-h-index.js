/**
 * @param {number[]} citations
 * @return {number}
 */
var hIndex = function(citations) {
    const sorted = citations.sort((a, b) => b - a)
    let i = 0

    while (i < citations[i]) {
        console.log(i)
        i++}
    return i
};